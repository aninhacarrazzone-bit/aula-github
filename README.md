# Aula de Git e GitHub

## Comandos Git
- `git --version`: Retorna a versão do Git instalado.
- `git clone URL`: Cria uma cópia local e um repoitório remoto. 
- `git add .`: Adiciona os arquivos modificados no próximo commit.
- `git commit -m "..."`: Fazer o commit com a mensagem.